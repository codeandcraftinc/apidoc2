#!/usr/bin/env node

'use strict';

require('babel-core/register')({
  plugins: [
    [
      'module-resolver',
      {
        alias: {
          _: './lib',
          test: './test'
        }
      }
    ]
  ],
  presets: [
    [
      'env',
      {
        targets: {
          node: 'current'
        }
      }
    ]
  ]
});

require('../lib');
